{
  "name": "library.js.coffee.2nd",
  "private": true,
  "version": "2.0.0",
  "description": "coffee framework second edition client js library & webmapper",
  "scripts": {
    "build:lib": "pnpm --filter @elpusk/lib build",
    "build:web": "pnpm --filter webmapper build",
    "build": "pnpm build:lib && pnpm build:web",
    "dev": "pnpm --filter webmapper dev",
    "preview": "pnpm --filter webmapper preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.27.0",
  "dependencies": {
    "new-package": "^0.1.0"
  }
}
